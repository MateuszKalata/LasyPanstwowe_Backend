# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    env_file:
      - database.env
    ports:
    - 8080:8080
    - 5432:5432
    volumes:
      - ./db.sql:/docker-entrypoint-initdb.d/init-db.sql
